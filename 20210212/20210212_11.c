/* Задача 11.  
Напишете програма, в която се въвеждат пореден номер,
име, фамилия, възраст на участници в маратон. Изискването
е за името и фамилията да не се използват масиви с
фиксирана дължина. След като въвеждането завърши,
изведете списък с участниците.  */
#include <stdio.h>
#include <stdlib.h>
#define MAX_NUM 100 /* assume this is the maximum number of participants */

void printstr(char *s);
void realloc_str(char *s, int size);
void get_name(char *name, int nameLenght);

int main(void) {
	char choice = 'y';
	int id[MAX_NUM], age[MAX_NUM];
	


	
	return 0;
}